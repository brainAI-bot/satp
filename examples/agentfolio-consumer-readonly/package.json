{
  "name": "@brainai/satp-agentfolio-consumer-readonly-example",
  "version": "0.0.0-example",
  "private": true,
  "description": "Offline AgentFolio-style SATP consumer example using unsigned identity attestation requests.",
  "type": "commonjs",
  "scripts": {
    "check": "find src tests -name '*.js' -type f -print0 | xargs -0 -n1 node --check",
    "example": "node src/readOnlyConsumerExample.js",
    "test": "node --test tests/*.test.js"
  }
}
